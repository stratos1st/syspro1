#include <iostream>

#include "./block/block.hpp"
#include "./wallet_struct/wallet_struct.hpp"
#include "./tree_node/tree_node.hpp"

int main(){

/*  tree_node *a=new tree_node(100);
  wallet_struct *c=new wallet_struct("stratos");

  block<tree_node> *b=new block<tree_node>(a);
  block<wallet_struct> *d=new block<wallet_struct>(c);

  b->printt();
  d->printt();
*/

  return 0;
}
